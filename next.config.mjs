/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['localhost','imts.ac.in', 'login.imtsinstitute.com'], // Add your external domain here
    }
};

export default nextConfig;
